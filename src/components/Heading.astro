---
const { title, subTitle, class: className, subHeading } = Astro.props;
---

<div
  class={`text-center flex flex-col items-center justify-center gap-6 py-10 px-4 ${className}`}
>
  <h1
    class={`md:text-4xl text-3xl md:leading-12 font-extrabold text-text !font-hero`}
  >
    <slot />
  </h1>
  {subHeading && <p>{subHeading}</p>}
</div>
